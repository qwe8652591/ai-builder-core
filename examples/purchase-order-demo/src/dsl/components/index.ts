/**
 * DSL 组件自动加载入口
 * 
 * 所有组件在导入时自动注册到 Component Registry
 */

// ==================== 业务组件 ====================
import './OrderCard.component';
import './StatusTag.component';

console.log('[DSL Components] 所有组件已自动加载并注册');

